version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - cd backend
      - npm install
  build:
    commands:
      - echo Copying backend files to root for Elastic Beanstalk deployment...
      - cp -r backend/* .

artifacts:
  files:
    - '**/*'
